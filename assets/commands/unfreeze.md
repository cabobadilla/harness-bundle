---
description: Release the freeze scope, restoring full edit access.
---

Remove `.claude/.freeze` if it exists. After this, edits are allowed
anywhere (within the standard permissions in `.claude/settings.json`).
Confirm the scope is released.
