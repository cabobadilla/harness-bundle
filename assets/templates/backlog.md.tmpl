# Backlog — {{PROJECT_NAME}}

Lista append-only de TODOs accionables. Las fuentes principales son:
- `/evaluate` (agrega secciones automáticamente con findings P0/P1/P2).
- El usuario (TODOs manuales).

El `/build` lee este archivo al inicio y resuelve los ítems abiertos (`- [ ]`)
además del spec. Cuando un ítem queda hecho, marcalo `- [x]` con una nota corta.

## TODO manual

<!-- agregá ítems manuales acá -->

<!-- /evaluate agregará secciones "## From /evaluate <slug>-<date>" abajo -->
